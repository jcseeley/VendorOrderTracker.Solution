@{
  Layout = "_Layout";
}

@using VendorOrderTracker.Models;
<h2>Vendors:</h2>
@if (Model.Count == 0)
{
  <h3>There are no vendors on your list.</h3>
}

@foreach (Vendor vendor in Model)
{
  <h3><a href="/vendors/@vendor.Id">@vendor.Name</a></h3>
}
<a href='/vendors/new'>Add A Vendor</a>